const {getPassword} = require('./5');

test('getPassword()', () => {
    // ~30s per run
    // expect(getPassword('abc')).toBe('18f47a30');
    // expect(getPassword('ojvtpuvg')).toBe('4543c154');
});